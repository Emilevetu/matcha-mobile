import React from 'react';
import { UserLocationProps } from '../Map.types';
// RNM affiche déjà l'user via showsUserLocation sur MapView.
// On garde un composant vide pour homogénéité API.
export function RNMUserLocation(_: UserLocationProps) {
  return null;
}
